// enums acting like state
enum DevEnvironment { staging, dev, production }

enum TabEnum { home, chats, profile, circles, wallet }

// view state
enum ViewState { busy, error, idle, success, exception }

// biometeric support enum
enum SupportState { unknown, supported }

// LoadingStateEnum
enum LoadingStateEnum{loading, success, error, network, unknown,init}